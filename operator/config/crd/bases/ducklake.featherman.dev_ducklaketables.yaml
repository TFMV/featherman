---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ducklaketables.ducklake.featherman.dev
spec:
  group: ducklake.featherman.dev
  names:
    kind: DuckLakeTable
    listKind: DuckLakeTableList
    plural: ducklaketables
    singular: ducklaketable
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.catalogRef
      name: Catalog
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DuckLakeTable is the Schema for the ducklaketables API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DuckLakeTableSpec defines the desired state of DuckLakeTable
            properties:
              catalogRef:
                description: CatalogRef references the DuckLakeCatalog this table
                  belongs to
                type: string
              columns:
                description: Columns defines the schema of the table
                items:
                  description: ColumnDefinition defines a column in the table
                  properties:
                    comment:
                      description: Comment provides documentation for the column
                      type: string
                    name:
                      description: Name of the column
                      pattern: '[a-zA-Z][a-zA-Z0-9_]*'
                      type: string
                    nullable:
                      description: Nullable specifies if the column can contain NULL
                        values
                      type: boolean
                    type:
                      description: Type is the SQL data type of the column
                      enum:
                      - INTEGER
                      - BIGINT
                      - DOUBLE
                      - BOOLEAN
                      - VARCHAR
                      - DATE
                      - TIMESTAMP
                      - DECIMAL
                      type: string
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              comment:
                description: Comment provides documentation for the table
                type: string
              format:
                description: Format specifies the Parquet format configuration
                properties:
                  compression:
                    description: Compression algorithm to use
                    enum:
                    - SNAPPY
                    - ZSTD
                    - NONE
                    type: string
                  rowGroupSize:
                    description: RowGroupSize specifies the size of row groups in
                      bytes
                    format: int64
                    minimum: 1048576
                    type: integer
                type: object
              mode:
                description: Mode specifies how to handle existing data
                enum:
                - append
                - overwrite
                type: string
              name:
                description: Name is the name of the table
                pattern: '[a-zA-Z][a-zA-Z0-9_]*'
                type: string
              partitioning:
                description: Partitioning defines the partition columns
                items:
                  type: string
                type: array
              ttlDays:
                description: TTLDays specifies the number of days to retain data
                format: int32
                minimum: 1
                type: integer
            required:
            - catalogRef
            - columns
            - name
            type: object
          status:
            description: DuckLakeTableStatus defines the observed state of DuckLakeTable
            properties:
              bytesWritten:
                description: BytesWritten is the total bytes written to this table
                format: int64
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the table's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAppliedSnapshot:
                description: LastAppliedSnapshot is the ID of the last successful
                  snapshot
                type: string
              lastModified:
                description: LastModified is when the table was last modified
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation that was acted
                  on
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the table
                enum:
                - Pending
                - Creating
                - Ready
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
